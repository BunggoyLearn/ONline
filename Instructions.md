# Instructions to run after a fresh git clone
### - Create a .env using the `.env.example` template
### - run `npm install`
### - run `psql -U postgres`
### - connect to database `\i db/schema.sql` then do `\q` to exit database
### - run `npm start`